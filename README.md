# IDE Code Assistant

**IDE Code Assistant** — это приложение, предоставляющее интерфейс для взаимодействия с моделями GPT от OpenAI или иного провайдера (например, LMStudio) через REST API, реализованный с помощью FastAPI. Приложение позволяет генерировать внутренние API-ключи для доступа к своим эндпоинтам, выполнять запросы к моделям, включает механизмы валидации и управления ключами доступа, а также поддерживает базу знаний с функциональностью поиска по ней.

---

## 🚀 Основные возможности

- **Генерация и управление внутренними API-ключами** для безопасного доступа к API вашего приложения.
- **Интеграция с OpenAI API** или **LMStudio API** для выполнения запросов к моделям GPT.
- **Обработка текстовых данных и файлов** (PDF, DOCX) для добавления в базу знаний.
- **Поиск по базе знаний** с использованием векторных эмбеддингов и индекса FAISS.
- **REST API с использованием FastAPI** для обработки запросов и ответа клиентам.
- **Тестирование с использованием Pytest** для обеспечения надежности и стабильности кода.
- **Веб-интерфейс для тестирования API** для удобного взаимодействия с эндпоинтами.

---

## 📚 Содержание

1. [Требования](#требования)
2. [Установка](#установка)
   - [Клонирование репозитория](#клонирование-репозитория)
   - [Создание и активация виртуального окружения](#создание-и-активация-виртуального-окружения)
   - [Установка зависимостей](#установка-зависимостей)
3. [Конфигурация](#конфигурация)
   - [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Запуск приложения](#запуск-приложения)
   - [Запуск сервера FastAPI](#запуск-сервера-fastapi)
   - [Использование API эндпоинтов](#использование-api-эндпоинтов)
   - [Веб-интерфейс для тестирования API](#веб-интерфейс-для-тестирования-api)
5. [Структура проекта](#структура-проекта)
6. [Запуск тестов](#запуск-тестов)
7. [Примечания](#примечания)
8. [Лицензия](#лицензия)

---

## 📋 Требования

- **Python 3.10** или выше
- `pip` — менеджер пакетов Python
- `virtualenv` — для создания виртуального окружения (рекомендуется)
- Аккаунт **OpenAI** и действительный **API-ключ** (если используете OpenAI)
- Развернутый и настроенный **LMStudio API** (если используете LMStudio)
- **Git** (для клонирования репозитория)

---

## 🛠️ Установка

### Клонирование репозитория

Клонируйте репозиторий проекта на ваш локальный компьютер:

```bash
git clone https://github.com/Bondartsov/IDE_code_assistant.git
cd IDE_code_assistant
```

### Создание и активация виртуального окружения

Рекомендуется использовать виртуальное окружение для изоляции зависимостей проекта.

Создайте виртуальное окружение:

```bash
python -m venv venv
```

Активируйте виртуальное окружение:

- **На Windows:**

  ```bash
  venv\Scripts\activate
  ```

- **На macOS/Linux:**

  ```bash
  source venv/bin/activate
  ```

### Установка зависимостей

Установите необходимые зависимости с помощью `pip`:

```bash
pip install -r requirements.txt
```

---

## ⚙️ Конфигурация

### Настройка переменных окружения

Создайте файл `.env` в корневом каталоге проекта и задайте необходимые переменные окружения.

Пример файла `.env`:

```ini
# Тип провайдера API: 'openai' или 'lmstudio'
API_PROVIDER=openai

# Ваш API-ключ OpenAI (замените 'your_openai_api_key_here' на ваш реальный API-ключ)
OPENAI_API_KEY=your_openai_api_key_here

# URL LMStudio API (если используете LMStudio)
LMSTUDIO_API_URL=http://localhost:8080/v1/completions

# Имя модели (по умолчанию 'chatgpt-4o-latest')
MODEL_NAME=chatgpt-4o-latest
```

**Важно:** Если вы используете OpenAI, обязательно укажите ваш действительный API-ключ в поле `OPENAI_API_KEY`. Ни в коем случае не храните реальный API-ключ в открытом доступе или не передавайте его другим лицам.

---

## 🚀 Запуск приложения

### Запуск сервера FastAPI

Для запуска сервера выполните следующую команду из корневого каталога проекта:

```bash
uvicorn main:app --reload
```

- Сервер запустится по адресу `http://127.0.0.1:8000`.
- Параметр `--reload` позволяет автоматически перезапускать сервер при изменениях в коде.

### Использование API эндпоинтов

После запуска сервера вы можете взаимодействовать с API вашего приложения.

- **Документация Swagger UI:** Перейдите по адресу `http://127.0.0.1:8000/docs` для просмотра и тестирования доступных эндпоинтов через веб-интерфейс.

- **Генерация API-ключа:**

  Отправьте POST-запрос на `/api/generate_key/` для получения нового API-ключа.

- **Использование API-ключа:**

  Для доступа к защищенным эндпоинтам необходимо передавать API-ключ в заголовке `api-key`.

### Веб-интерфейс для тестирования API

Вы можете использовать удобный веб-интерфейс для тестирования эндпоинтов без необходимости использовать Swagger UI или другие инструменты.

1. **Настройте статический хостинг для папки `api_tester`:**

   - Вы можете запустить простой HTTP-сервер с помощью Python:

     ```bash
     cd api_tester
     python -m http.server 8001
     ```

   - Откройте в браузере: `http://127.0.0.1:8001`

2. **Используйте интерфейс для выбора эндпоинтов, ввода данных и отправки запросов.**

---

## 📁 Структура проекта

- **`main.py`** — точка входа в приложение FastAPI.
- **`requirements.txt`** — список зависимостей проекта.
- **`.env`** — файл с переменными окружения (не включается в систему контроля версий).
- **`api/`** — пакет с определениями эндпоинтов API.
  - `__init__.py`
  - `endpoints.py`
  - `models.py`
- **`core/`** — пакет с основными компонентами приложения.
  - `__init__.py`
  - `config.py` — загрузка настроек и переменных окружения.
  - `database.py` — настройка базы данных и моделей SQLAlchemy.
  - `security.py` — управление API-ключами и безопасность.
  - `logger.py` — настройка логирования.
- **`services/`** — пакет с сервисами приложения.
  - `__init__.py`
  - `openai_service.py` — взаимодействие с OpenAI API и LMStudio.
  - `embedding_service.py` — генерация эмбеддингов.
  - `indexing_service.py` — управление индексом FAISS.
  - `file_service.py` — обработка файлов (PDF, DOCX).
- **`tests/`** — пакет с тестами для приложения.
  - `__init__.py`
  - `test_api.py`
- **`api_tester/`** — веб-интерфейс для тестирования API.
  - `index.html`
  - `app.js`
  - `styles.css`
  - `endpoints.json`

---

## ✅ Запуск тестов

Для запуска тестов используйте команду:

```bash
pytest
```п

- Убедитесь, что вы активировали виртуальное окружение перед запуском тестов.
- Тесты находятся в папке `tests/` и обеспечивают проверку основных функций приложения.

---

## ⚠️ Примечания

- **Безопасность API-ключей:**

  - Никогда не передавайте ваш реальный API-ключ другим лицам.
  - Убедитесь, что файл `.env` не включен в систему контроля версий (добавьте его в `.gitignore`).

- **Использование LMStudio:**

  - При использовании LMStudio убедитесь, что сервис запущен и доступен по указанному URL.
  - Обратите внимание, что некоторые функции (например, генерация эмбеддингов) могут быть недоступны при использовании LMStudio.

- **Лимиты и ограничения OpenAI API:**

  - Следите за использованием вашего API-ключа OpenAI, чтобы избежать превышения лимитов и дополнительных затрат.
  - Ознакомьтесь с политикой использования OpenAI API.

---

## 📄 Лицензия

Этот проект лицензирован на условиях лицензии MIT.

---

**При возникновении вопросов или предложений по улучшению проекта, пожалуйста, создайте issue или pull request в репозитории: [https://github.com/Bondartsov/IDE_code_assistant](https://github.com/Bondartsov/IDE_code_assistant).**

---

**Желаем успехов в использовании IDE Code Assistant!**